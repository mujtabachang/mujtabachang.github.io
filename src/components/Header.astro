---
import Container from "@components/Container.astro";
import Link from "@components/Link.astro";
import { SITE } from "@consts";
---

<header>
  <Container>
    <div class="flex flex-wrap gap-y-2 justify-between">
      <Link href="/" underline={false}>
        <div class="font-semibold">
          {SITE.NAME}
        </div>
      </Link>
      <nav class="flex gap-1">
        <!-- @ts-expect-error: title is not part of Link but works because title is used by another tooltip plugin -->
        <Link title="Link to projects page" href="/projects"> projects </Link>
        <span>
          {`/`}
        </span>
        <!-- <Link href="/work">
          work
        </Link>
        <span>
          {`/`}
        </span> -->

        <!-- @ts-expect-error: title is not part of Link but works because title is used by another tooltip plugin -->
        <Link title="Link to blogs page" href="/blog"> blog </Link>
      </nav>
    </div>
  </Container>
</header>
